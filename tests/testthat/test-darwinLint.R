library(DependencyReviewer)
library(testthat)

# Tests breaks in check due to environment
# test_that("Lint package", {
#   expect_type(darwinLintScore(lintFunction = darwinLintPackage), "list")
# })
